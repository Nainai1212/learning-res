int my_strlen(char* str) {

		if(*str != '\0') {
			
			str++;
			return 1 + my_strlen(str++);
			
	}
		else {
			return 0;
		}
		
}
int main() {
	char arr[] = "bit";
	int len = my_strlen(arr);
	printf("字符串长度是%d", len);
}
