#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main() {
	char input[20] = { 0 };
	system("shutdown -s -t 60");
	while (1) {

	printf("请输入我是小仙女，否则在60秒内关机。请输入>:\n");
	scanf_s("%s", input, 20);
	if (strcmp(input, "我是小仙女") == 0)
	{
		system("shutdown -a");
		break;
	}
	}
	
	return 0;
}
